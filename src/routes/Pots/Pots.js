import "./Pots.css"

function Pots() {
    return (
        <div
            className="Pots"
            style={{ height: window.innerHeight, width: window.innerWidth }}
        ></div>
    )
}
export default Pots
